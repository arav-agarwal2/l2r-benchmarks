name:
  ActorCritic
config:
  activation: SELU
  state_dim: 32
  action_dim: 2
  fusion_hiddens:
    - 64
    - 32
    - 32
    - 16
  critic_cfg:
    name:
      Qfunction
    config:
      use_speed:
        True
  use_speed:
    True
